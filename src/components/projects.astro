<section
  id="projects"
  class="py-12 border-t border-[#ffffff10] text-[var(--white)]"
>
  <div class="max-w-5xl mx-auto">
    <h2 class="text-lg text-[var(--sec)] mb-2 shiny-sec">Projects</h2>
    <h3 class="text-3xl md:text-5xl font-medium mb-8">
      Under construction
    </h3>

    <article class="border border-[var(--white-icon-tr)] rounded-xl bg-[#1414149c] p-6 md:p-8">
      <div class="flex flex-col md:flex-row md:items-center justify-between gap-6">
        <div class="space-y-3">
          <p class="text-2xl font-semibold">Good projects take a minute.</p>
          <p class="text-[var(--white-icon)] leading-relaxed max-w-2xl">
            I am rebuilding this section around work that is current, useful,
            and worth explaining properly. Old proof-of-concepts are staying in
            the workshop until they earn a spot here.
          </p>
        </div>
        <div class="flex flex-wrap gap-2 md:justify-end">
          {
            ["AppSec", "Automation", "Homelab", "AI tooling"].map((tag) => (
              <span class="text-xs text-[var(--white)] border border-[var(--white-icon-tr)] rounded-full px-3 py-1">
                {tag}
              </span>
            ))
          }
        </div>
      </div>
    </article>
  </div>
</section>
